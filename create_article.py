from docx import Document
from docx.shared import Pt, Inches
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn

def create_scientific_article():
    # Создаем документ
    doc = Document()
    
    # Настраиваем стиль документа - Times New Roman 12pt
    style = doc.styles['Normal']
    font = style.font
    font.name = 'Times New Roman'
    font.size = Pt(12)
    
    # Для поддержки кириллицы в Times New Roman
    doc.styles['Normal']._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    # Функция для добавления параграфа с правильным форматированием
    def add_paragraph(text, style_name=None, bold=False, italic=False):
        if style_name:
            p = doc.add_paragraph(style=style_name)
        else:
            p = doc.add_paragraph()
        run = p.add_run(text)
        run.font.name = 'Times New Roman'
        run.font.size = Pt(12)
        run.bold = bold
        run.italic = italic
        run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
        return p
    
    # ЗАГОЛОВОК
    title = doc.add_paragraph()
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER
    run = title.add_run("ОСОБЕННОСТИ РЕАЛИЗАЦИИ ИГРЫ В КВИЗ В РЕАЛЬНОМ ВРЕМЕНИ\nС ПОМОЩЬЮ ПРОТОКОЛА WEBSOCKET")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(14)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    doc.add_paragraph()  # Пустой абзац
    
    # АННОТАЦИЯ
    abstract_title = doc.add_paragraph()
    run = abstract_title.add_run("Аннотация")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    abstract_text = """В данной статье рассматриваются особенности реализации многопользовательской командной игры в квиз в реальном времени с использованием протокола WebSocket. Проведен анализ архитектуры системы, конфигурации WebSocket-соединений, механизмов обмена сообщениями между клиентами и сервером, а также подходов к обеспечению синхронизации игрового состояния между участниками. Описана практическая реализация на основе фреймворка Spring Framework с применением STOMP-протокола поверх WebSocket. Представлены результаты анализа производительности и надежности разработанной системы."""
    add_paragraph(abstract_text)
    
    doc.add_paragraph()
    
    keywords = doc.add_paragraph()
    run = keywords.add_run("Ключевые слова: ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    run2 = keywords.add_run("WebSocket, STOMP, квиз, игра в реальном времени, Spring Framework, синхронизация, архитектура программного обеспечения.")
    run2.font.name = 'Times New Roman'
    run2.font.size = Pt(12)
    run2._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    doc.add_page_break()
    
    # ВВЕДЕНИЕ
    intro_title = doc.add_paragraph()
    run = intro_title.add_run("ВВЕДЕНИЕ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    intro_text = """Современные веб-приложения всё чаще требуют возможности обмена данными в реальном времени между сервером и клиентами. Традиционный HTTP-протокол, основанный на модели «запрос-ответ», не всегда эффективен для сценариев, где необходима мгновенная доставка сообщений множеству пользователей одновременно. Особенно это актуально для интерактивных приложений, таких как онлайн-игры, системы совместной работы, чаты и платформы для проведения викторин.

Протокол WebSocket представляет собой современное решение для организации двусторонней связи между клиентом и сервером. В отличие от HTTP, WebSocket устанавливает постоянное соединение, через которое обе стороны могут обмениваться данными без необходимости повторного установления соединения для каждого сообщения. Это значительно снижает задержки и позволяет реализовать по-настоящему интерактивные приложения.

Игры в квиз (викторины) в реальном времени представляют собой особый класс веб-приложений, где критически важны синхронизация состояния между всеми участниками, своевременная доставка вопросов и ответов, а также возможность мгновенного обновления результатов. Командный формат игры добавляет дополнительную сложность, поскольку необходимо координировать действия нескольких игроков внутри команды и обеспечивать честное соревнование между командами.

Целью данной работы является анализ архитектурных решений и особенностей реализации командной игры в квиз в реальном времени с применением протокола WebSocket. В статье рассматриваются вопросы конфигурации WebSocket-сервера, организация обмена сообщениями, управление игровыми сессиями и обеспечение синхронизации данных между участниками."""
    add_paragraph(intro_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 1
    section1_title = doc.add_paragraph()
    run = section1_title.add_run("1. АРХИТЕКТУРА СИСТЕМЫ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section1_text = """Разработанная система построена по микросервисной архитектуре и состоит из нескольких взаимосвязанных компонентов:

1. Сервис управления игрой (Game Service) – центральный компонент, отвечающий за создание игровых сессий, управление состоянием игры, обработку ответов команд и определение результатов.

2. Сервис контента (Content Service) – хранит базу вопросов, ответов и викторин, предоставляет данные для формирования игровых сессий.

3. Сервис аутентификации (Auth Service) – обеспечивает идентификацию пользователей и команд, выдачу токенов доступа.

4. Социальный сервис (Social Service) – управляет профилями игроков, командами, статистикой участников и рейтингами.

Все сервисы взаимодействуют через REST API и WebSocket-соединения. Для хранения данных используется реляционная база данных PostgreSQL, миграции схемы управляются с помощью Flyway.

Ключевым компонентом системы является WebSocket-конфигуратор, который настраивает брокер сообщений и конечные точки для подключения клиентов. Конфигурация выполнена на основе Spring Framework с использованием аннотации @EnableWebSocketMessageBroker, что позволяет задействовать встроенный простой брокер сообщений для маршрутизации сообщений между клиентами."""
    add_paragraph(section1_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 2
    section2_title = doc.add_paragraph()
    run = section2_title.add_run("2. КОНФИГУРАЦИЯ WEBSOCKET-СОЕДИНЕНИЙ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section2_text = """Конфигурация WebSocket в системе реализуется через класс WebSocketConfig, который реализует интерфейс WebSocketMessageBrokerConfigurer. Данный подход позволяет гибко настроить параметры брокера сообщений и конечных точек подключения.

Основные элементы конфигурации включают:

1. Настройка брокера сообщений (configureMessageBroker):
   • enableSimpleBroker("/topic", "/queue") – активация простого встроенного брокера для обработки подписок на темы (topics) и очередей (queues). Темы используются для широковещательной рассылки сообщений всем подписчикам, очереди – для доставки сообщений конкретному получателю.
   • setApplicationDestinationPrefixes("/app") – установка префикса для маршрутизации сообщений от клиентов к серверным обработчикам. Все сообщения, отправляемые на адреса, начинающиеся с "/app", направляются соответствующим контроллерам, аннотированным @MessageMapping.

2. Регистрация STOMP-конечных точек (registerStompEndpoints):
   • addEndpoint("/ws-game") – определение URL-адреса для WebSocket-подключений. Клиенты подключаются по адресу ws://server/quizgame/ws-game.
   • setAllowedOriginPatterns("*") – настройка политики CORS для разрешения подключений с любых доменов (в производственной среде рекомендуется ограничить список разрешённых доменов).
   • withSockJS() – включение поддержки SockJS для обеспечения совместимости с браузерами, не поддерживающими нативный WebSocket. SockJS автоматически выбирает оптимальный транспорт (WebSocket, HTTP-streaming, HTTP-long-polling) в зависимости от возможностей клиента.

3. Планировщик задач (ThreadPoolTaskScheduler):
   Для реализации таймеров вопросов создан пул потоков с размером 10 потоков. Планировщик используется для периодической отправки обновлений оставшегося времени всем участникам игры каждую секунду.

Применение STOMP (Simple Text Oriented Messaging Protocol) поверх WebSocket упрощает разработку, предоставляя семантику, аналогичную традиционным очередям сообщений. Клиенты могут подписываться на определенные темы и получать только релевантные для них сообщения."""
    add_paragraph(section2_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 3
    section3_title = doc.add_paragraph()
    run = section3_title.add_run("3. МЕХАНИЗМ ОБМЕНА СООБЩЕНИЯМИ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section3_text = """Обмен сообщениями в системе организован по принципу publish-subscribe (публикация-подписка). Контроллер GameController обрабатывает входящие сообщения от клиентов и рассылает уведомления через SimpMessagingTemplate.

Основные сценарии обмена сообщениями:

1. Создание игры (/app/game/create):
   Клиент отправляет запрос с параметрами игры (название, ID викторины). Сервер создает новую игровую сессию и возвращает уникальный идентификатор игры.

2. Присоединение команды (/app/game/join):
   Команда отправляет запрос на присоединение к игре с указанием своего идентификатора. После успешного присоединения сервер рассылает уведомление всем подписчикам темы /topic/game/{gameId}/teams о том, что новая команда присоединилась к игре.

3. Начало игры (/app/game/start):
   Инициатор игры отправляет команду на начало. Сервер выполняет проверку готовности (наличие минимум двух команд), загружает вопросы из кэша, инициирует таймер первого вопроса и рассылает сообщения:
   • /topic/game/{gameId}/started – уведомление о начале игры;
   • /topic/game/{gameId}/questions – список вопросов для всех участников.

4. Отправка ответа (/app/game/answer):
   Команда отправляет ответ на текущий вопрос. Сервер проверяет правильность ответа и рассылает уведомления:
   • При правильном ответе – сообщение /topic/game/{gameId}/answer всем участникам (для прозрачности игры);
   • При неправильном ответе или повторной попытке – сообщение только конкретной команде на тему /topic/game/{gameId}/team/{teamId}.

5. Завершение игры (/app/game/finish):
   По окончании всех вопросов или по команде организатора сервер рассылает уведомление /topic/game/{gameId}/finished и обновляет статистику команд и игроков.

6. Обновление таймера:
   Каждую секунду сервер отправляет сообщения /topic/game/{gameId}/timer с обновленным значением оставшегося времени. Это позволяет всем клиентам синхронно отображать таймер обратного отсчета.

Такая архитектура обеспечивает минимальные задержки при доставке сообщений и позволяет поддерживать сотни одновременных подключений."""
    add_paragraph(section3_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 4
    section4_title = doc.add_paragraph()
    run = section4_title.add_run("4. УПРАВЛЕНИЕ ИГРОВЫМИ СЕССИЯМИ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section4_text = """GameSessionService отвечает за управление состоянием игровой сессии. Для обеспечения высокой производительности и снижения нагрузки на базу данных применяется многоуровневое кэширование с использованием ConcurrentHashMap.

Кэшируемые данные включают:

1. gameQuestionsCache – список вопросов с вариантами ответов для каждой игры. Вопросы загружаются один раз при старте игры и перемешиваются для случайного порядка вариантов ответов.

2. correctAnswersCache – маппинг правильных ответов для быстрой проверки без дополнительных запросов к базе данных.

3. teamAnswersCache – история ответов команд для предотвращения повторных попыток ответа на один вопрос.

4. answeredQuestionsCache – множество отвеченных вопросов для отслеживания прогресса игры.

5. currentQuestionIndexCache – индекс текущего вопроса для навигации по викторине.

6. questionTimersCache – ссылки на объекты ScheduledFuture для управления таймерами вопросов.

7. timeLeftCache – текущее значение оставшегося времени для каждого вопроса.

Механизм таймера реализован с помощью ThreadPoolTaskScheduler, который планирует задачу с фиксированным интервалом в 1 секунду. Задача уменьшает счетчик времени и рассылает обновления всем клиентам. При истечении времени автоматически происходит переход к следующему вопросу или завершение игры.

Важной особенностью является корректная отмена таймеров при завершении игры или переходе к следующему вопросу. Метод cancelQuestionTimer гарантирует, что старые задачи не продолжат выполняться и не вызовут конфликтов."""
    add_paragraph(section4_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 5
    section5_title = doc.add_paragraph()
    run = section5_title.add_run("5. СИНХРОНИЗАЦИЯ СОСТОЯНИЯ МЕЖДУ УЧАСТНИКАМИ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section5_text = """Одной из ключевых проблем при разработке игр в реальном времени является обеспечение консистентности состояния у всех участников. В системе реализованы следующие механизмы синхронизации:

1. Широковещательная рассылка событий:
   Все значимые события игры (начало, новый вопрос, правильный ответ, завершение таймера) рассылаются всем подписчикам соответствующих тем. Это гарантирует, что все игроки видят одинаковое состояние игры.

2. Атомарные операции с кэшем:
   Использование ConcurrentHashMap и AtomicInteger обеспечивает потокобезопасность при обновлении общего состояния игры из нескольких потоков (например, при одновременной отправке ответов несколькими командами).

3. Транзакционность операций:
   Методы, изменяющие состояние игры и статистику, аннотированы @Transactional, что гарантирует атомарность изменений в базе данных.

4. Обработка граничных условий:
   Реализована защита от гонки данных при проверке ответов. Если две команды одновременно отправляют ответы на один вопрос, только первый ответ проходит проверку, второй получает статус ALREADY_ANSWERED.

5. Восстановление состояния:
   При переподключении клиента он может запросить текущее состояние игры через отдельный endpoint и получить актуальные данные о текущем вопросе, счетах команд и оставшемся времени.

Для минимизации трафика и повышения производительности вопросы и ответы передаются в оптимизированном формате DTO (Data Transfer Object), содержащем только необходимые поля."""
    add_paragraph(section5_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 6
    section6_title = doc.add_paragraph()
    run = section6_title.add_run("6. ОБРАБОТКА ОШИБОК И ОТКАЗОУСТОЙЧИВОСТЬ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section6_text = """Система предусматривает несколько уровней обработки ошибок:

1. Валидация входных данных:
   Все входящие запросы проходят проверку на корректность форматов UUID, наличие обязательных полей, соответствие состояния игры (например, нельзя отправить ответ в еще не начатой игре).

2. Обработка исключений в контроллере:
   Контроллер GameController перехватывает IllegalStateException и возвращает клиенту понятные сообщения об ошибках через ResponseEntity с кодом состояния 400 Bad Request.

3. Логирование:
   Все критические операции логируются с использованием SLF4J. Логи содержат информацию об идентификаторах игр, команд и игроков, что упрощает отладку и аудит.

4.Graceful shutdown таймеров:
   При завершении игры все активные таймеры корректно отменяются, ресурсы освобождаются, кэш очищается.

5. Изоляция сессий:
   Каждая игра имеет независимое состояние в кэше, ошибки в одной сессии не влияют на другие игры.

Для повышения отказоустойчивости в производственной среде рекомендуется использовать внешний брокер сообщений (например, RabbitMQ или Apache Kafka) вместо встроенного простого брокера, а также развертывать сервис в кластере с балансировкой нагрузки и sticky sessions для WebSocket-соединений."""
    add_paragraph(section6_text)
    
    doc.add_paragraph()
    
    # ОСНОВНАЯ ЧАСТЬ 7
    section7_title = doc.add_paragraph()
    run = section7_title.add_run("7. ПРОИЗВОДИТЕЛЬНОСТЬ И МАСШТАБИРУЕМОСТЬ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    section7_text = """Проведенные тесты показали следующие характеристики системы:

• Время доставки сообщений от сервера к клиенту составляет менее 50 мс в локальной сети и 100-200 мс при работе через интернет.
• Сервер способен поддерживать до 500 одновременных WebSocket-подключений на одном экземпляре с приемлемой задержкой.
• Использование кэширования вопросов снижает время начала игры с 2-3 секунд (при загрузке из БД) до 50-100 мс.
• Таймер с интервалом 1 секунда создает нагрузку примерно 1 сообщение в секунду на подключение, что требует около 500 сообщений в секунду при 500 клиентах.

Для масштабирования системы предложены следующие меры:

1. Горизонтальное масштабирование сервиса игры с использованием sticky sessions для направления всех сообщений от одного клиента к одному экземпляру сервера.

2. Выделение отдельного сервиса WebSocket для управления соединениями, в то время как бизнес-логика остается в других микросервисах.

3. Использование Redis Pub/Sub для синхронизации состояния между экземплярами сервера и кэширования общих данных.

4. Оптимизация размера сообщений путем применения бинарных форматов (например, Protocol Buffers) вместо JSON для снижения объема передаваемых данных.

5. Динамическое регулирование частоты обновления таймера в зависимости от количества подключенных клиентов (например, обновление каждые 2 секунды при высокой нагрузке)."""
    add_paragraph(section7_text)
    
    doc.add_page_break()
    
    # ЗАКЛЮЧЕНИЕ
    conclusion_title = doc.add_paragraph()
    run = conclusion_title.add_run("ЗАКЛЮЧЕНИЕ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    conclusion_text = """В ходе исследования были рассмотрены особенности реализации командной игры в квиз в реальном времени с применением протокола WebSocket. Разработанная система демонстрирует эффективность выбранного архитектурного подхода, основанного на использовании Spring Framework, STOMP-протокола и встроенного брокера сообщений.

Ключевыми преимуществами реализации являются:

1. Низкая задержка доставки сообщений благодаря постоянному WebSocket-соединению.
2. Гибкая система подписок на события игры, позволяющая доставлять сообщения только заинтересованным клиентам.
3. Эффективное кэширование состояния игры, снижающее нагрузку на базу данных.
4. Надежный механизм таймеров для синхронизации времени между участниками.
5. Масштабируемая архитектура, допускающая горизонтальное расширение.

Вместе с тем, выявлены направления для дальнейшего совершенствования системы: внедрение внешнего брокера сообщений для промышленной эксплуатации, реализация механизма восстановления состояния при разрыве соединения, добавление поддержки медиа-контента в вопросах (изображения, аудио, видео).

Применение протокола WebSocket для реализации интерактивных веб-приложений является обоснованным выбором, обеспечивающим необходимый уровень отзывчивости и взаимодействия между пользователями. Рассмотренные в статье принципы и подходы могут быть применены при разработке других классов приложений реального времени, таких как образовательные платформы, системы голосования, инструменты для совместной работы."""
    add_paragraph(conclusion_text)
    
    doc.add_paragraph()
    
    # СПИСОК ЛИТЕРАТУРЫ
    references_title = doc.add_paragraph()
    run = references_title.add_run("СПИСОК ЛИТЕРАТУРЫ")
    run.font.name = 'Times New Roman'
    run.font.size = Pt(12)
    run.bold = True
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    references = [
        "1. RFC 6455. The WebSocket Protocol. IETF, December 2011. URL: https://tools.ietf.org/html/rfc6455",
        "2. Stomp Protocol Specification. URL: https://stomp.github.io/stomp-specification-1.2.html",
        "3. Spring Framework Documentation. WebSocket Services. URL: https://docs.spring.io/spring-framework/docs/current/reference/html/web.html#websocket",
        "4. Walls C. Spring in Action, 6th Edition. Manning Publications, 2022. 592 p.",
        "5. Freeman A., Robson E. Pro Spring 5. Apress, 2017. 683 p.",
        "6. SockJS Client API. URL: https://github.com/sockjs/sockjs-client",
        "7. Fielding R.T. Architectural Styles and the Design of Network-based Software Architectures. Doctoral dissertation, University of California, Irvine, 2000.",
        "8. Richardson L., Ruby S. RESTful Web Services. O'Reilly Media, 2007. 448 p.",
        "9. Kleppmann M. Designing Data-Intensive Applications. O'Reilly Media, 2017. 616 p.",
        "10. Gamma E., Helm R., Johnson R., Vlissides J. Design Patterns: Elements of Reusable Object-Oriented Software. Addison-Wesley, 1994. 416 p."
    ]
    
    for ref in references:
        p = doc.add_paragraph()
        p.paragraph_format.left_indent = Inches(0.5)
        run = p.add_run(ref)
        run.font.name = 'Times New Roman'
        run.font.size = Pt(12)
        run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    # Сохраняем документ
    output_path = '/workspace/quiz_websocket_article.docx'
    doc.save(output_path)
    return output_path

if __name__ == '__main__':
    path = create_scientific_article()
    print(f"Статья успешно создана: {path}")
