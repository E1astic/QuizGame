server:
  port: 8040
  servlet:
    context-path: /quizgame
spring:
  application:
    name: quiz-game
  flyway:
    enabled: true
    validate-on-migrate: true
  datasource:
    url: jdbc:postgresql://localhost:5433/quizgame
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

auth:
  jwt:
    secret: T,Rn[}@24!SVn]3F)i<D]IJT;6:t}rTz$6i3&p41B=M
    lifetime: 1h

websocket:
  allowed-origins: "*"

logging:
  level:
    ru.fil.game_service: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.messaging: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
